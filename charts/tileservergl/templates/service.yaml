apiVersion: v1
kind: Service
metadata:
  name: {{ include "tileservergl.fullname" . }}
  labels:
    {{- include "tileservergl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tileservergl.selectorLabels" . | nindent 4 }}
