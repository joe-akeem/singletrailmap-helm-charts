apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tileservergl.fullname" . }}-test-connection"
  labels:
    {{- include "tileservergl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tileservergl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
